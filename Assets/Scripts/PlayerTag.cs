using UnityEngine;
using System.Collections;


class PlayerTag : MonoBehaviour
{
   

    // Use this for initialization
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
    void OnGUI()
    {
      //  GUI.Label(new Rect(-20, 20, 100, 50), GameStatus.GetInstance().GetUserName());
       
    }
}

